@model IEnumerable<Role>

@{
    ViewBag.Title = "Управление на изтритите длъжности";
}
<h1 class="h3 mb-4">Изтрити длъжности</h1>

<p>
    <a asp-action="Index" class="btn btn-success mb-3">Виж актуални</a>
</p>

@if (!Model.Any(d => d.IsDeleted))
{
    <div class="alert alert-info">Няма изтрити длъжности.</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-bordered align-middle table-data">
            <thead class="table-light">
                <tr>
                    <th>Име</th>
                    @if (User.IsInRole("SuperAdmin") || User.IsInRole("Admin"))
                    {
                        <th class="action-headings-table">Действия</th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (var role in Model.Where(d => d.IsDeleted).OrderBy(d => d.Name))
                {
                    <tr>
                        <td>@role.Name</td>
                        @if (User.IsInRole("SuperAdmin") || User.IsInRole("Admin"))
                        {
                            <td class="d-flex justify-content-center">
                                <a asp-action="Restore" asp-route-id="@role.Id" class="btn btn-sm btn-primary m-1">Възстанови</a>
                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
}